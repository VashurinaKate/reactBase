import { render } from "@testing-library/react";
import { Profile } from "../index";

describe('Profile', () => {
    // it('matches snapshot', () => {
    //     const profile = render(<Profile></Profile>);

    //     expect(profile).toMatchSnapshot()
    // })
})
