import { render, screen } from "@testing-library/react";
import { AddChat } from "../AddChat";

describe('AddChat', () => {
    // it('matches snapshot', () => {
    //     const addChat = render(<AddChat>{() => <form/>}</AddChat>);

    //     expect(addChat).toMatchSnapshot()
    // })

    // it('calls onAddChat when clicked', () => {
    //     const handleClick = jest.fn()
    //     render(<AddChat onAddChat={handleClick}>{() => <form>text</form>}</AddChat>)
        
    //     const clickable = screen.getByText('text')
    //     clickable.click()

    //     expect(handleClick).toHaveBeenCalledTimes(1)
    // })
})
